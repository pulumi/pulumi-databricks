// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * [![Public Preview](https://img.shields.io/badge/Release_Stage-Public_Preview-yellowgreen)](https://docs.databricks.com/aws/en/release-notes/release-types)
 *
 * [API Documentation](https://docs.databricks.com/api/workspace/aisearch)
 *
 * This data source can be used to get a single AI Search index by name.
 *
 * ## Example Usage
 *
 * # Example: AI Search Index Datasource (Singular)
 */
export function getAiSearchIndex(args: GetAiSearchIndexArgs, opts?: pulumi.InvokeOptions): Promise<GetAiSearchIndexResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("databricks:index/getAiSearchIndex:getAiSearchIndex", {
        "name": args.name,
        "providerConfig": args.providerConfig,
    }, opts);
}

/**
 * A collection of arguments for invoking getAiSearchIndex.
 */
export interface GetAiSearchIndexArgs {
    /**
     * Name of the AI Search index. Server-assigned full resource path
     * (`workspaces/{workspace}/endpoints/{endpoint}/indexes/{index}`) on output, where
     * `{index}` is the index's Unity Catalog table name. On create, the user-supplied UC
     * table name is conveyed via `CreateIndexRequest.index_id`; the server composes the
     * full `name` and returns it on the response
     */
    name: string;
    /**
     * Configure the provider for management through account provider.
     */
    providerConfig?: inputs.GetAiSearchIndexProviderConfig;
}

/**
 * A collection of values returned by getAiSearchIndex.
 */
export interface GetAiSearchIndexResult {
    /**
     * (string) - Creator of the index
     */
    readonly creator: string;
    /**
     * (DeltaSyncIndexSpec) - Specification for a Delta Sync index. Set when `indexType` is `DELTA_SYNC`
     */
    readonly deltaSyncIndexSpec: outputs.GetAiSearchIndexDeltaSyncIndexSpec;
    /**
     * (DirectAccessIndexSpec) - Specification for a Direct Access index. Set when `indexType` is `DIRECT_ACCESS`
     */
    readonly directAccessIndexSpec: outputs.GetAiSearchIndexDirectAccessIndexSpec;
    /**
     * (string) - Name of the endpoint associated with the index. Ignored on create — the endpoint is
     * taken from `CreateIndexRequest.parent`; populated only on output
     */
    readonly endpoint: string;
    /**
     * (string) - The subtype of the index. Set on create and immutable thereafter. Possible values are: `FULL_TEXT`, `HYBRID`, `VECTOR`
     */
    readonly indexSubtype: string;
    /**
     * (string) - Type of index. Required on create and immutable thereafter. Possible values are: `DELTA_SYNC`, `DIRECT_ACCESS`
     */
    readonly indexType: string;
    /**
     * (string) - Name of the column
     */
    readonly name: string;
    /**
     * (string) - Primary key of the index. Set on create and immutable thereafter
     */
    readonly primaryKey: string;
    readonly providerConfig?: outputs.GetAiSearchIndexProviderConfig;
    /**
     * (IndexStatus) - Current status of the index
     */
    readonly status: outputs.GetAiSearchIndexStatus;
}
/**
 * [![Public Preview](https://img.shields.io/badge/Release_Stage-Public_Preview-yellowgreen)](https://docs.databricks.com/aws/en/release-notes/release-types)
 *
 * [API Documentation](https://docs.databricks.com/api/workspace/aisearch)
 *
 * This data source can be used to get a single AI Search index by name.
 *
 * ## Example Usage
 *
 * # Example: AI Search Index Datasource (Singular)
 */
export function getAiSearchIndexOutput(args: GetAiSearchIndexOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetAiSearchIndexResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("databricks:index/getAiSearchIndex:getAiSearchIndex", {
        "name": args.name,
        "providerConfig": args.providerConfig,
    }, opts);
}

/**
 * A collection of arguments for invoking getAiSearchIndex.
 */
export interface GetAiSearchIndexOutputArgs {
    /**
     * Name of the AI Search index. Server-assigned full resource path
     * (`workspaces/{workspace}/endpoints/{endpoint}/indexes/{index}`) on output, where
     * `{index}` is the index's Unity Catalog table name. On create, the user-supplied UC
     * table name is conveyed via `CreateIndexRequest.index_id`; the server composes the
     * full `name` and returns it on the response
     */
    name: pulumi.Input<string>;
    /**
     * Configure the provider for management through account provider.
     */
    providerConfig?: pulumi.Input<inputs.GetAiSearchIndexProviderConfigArgs | undefined>;
}
