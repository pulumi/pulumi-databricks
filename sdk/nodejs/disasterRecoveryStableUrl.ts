// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * [![Public Preview](https://img.shields.io/badge/Release_Stage-Public_Preview-yellowgreen)](https://docs.databricks.com/aws/en/release-notes/release-types)
 *
 * [API Documentation](https://docs.databricks.com/api/account/disasterrecovery)
 *
 * A stable URL is a region-independent, failover-aware endpoint for accessing a workspace. It always routes to whichever workspace is currently the primary, so downstream tools and clients do not need to change their connection strings when a failover happens.
 *
 * The URL is generated by the backend at creation time and is immutable thereafter. A stable URL can be attached to a failover group (via the failover group's `workspace_sets.stable_url_names`) so that its routing target follows the group's primary across failovers.
 *
 * > **Note** This resource can only be used with an account-level provider!
 *
 * ## Example Usage
 *
 * Creating a stable URL bound to an initial workspace:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as databricks from "@pulumi/databricks";
 *
 * const _this = new databricks.DisasterRecoveryStableUrl("this", {
 *     stableUrlId: "accounting-stable-url",
 *     initialWorkspaceId: "1234567890123456",
 * });
 * ```
 */
export class DisasterRecoveryStableUrl extends pulumi.CustomResource {
    /**
     * Get an existing DisasterRecoveryStableUrl resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DisasterRecoveryStableUrlState, opts?: pulumi.CustomResourceOptions): DisasterRecoveryStableUrl {
        return new DisasterRecoveryStableUrl(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'databricks:index/disasterRecoveryStableUrl:DisasterRecoveryStableUrl';

    /**
     * Returns true if the given object is an instance of DisasterRecoveryStableUrl.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DisasterRecoveryStableUrl {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DisasterRecoveryStableUrl.__pulumiType;
    }

    /**
     * (string) - Fully qualified resource name of the FailoverGroup this stable URL is
     * currently linked to, in the format
     * `accounts/{account_id}/failover-groups/{failover_group_id}`. Empty when
     * the stable URL is not attached to any failover group
     */
    declare public /*out*/ readonly failoverGroupName: pulumi.Output<string>;
    /**
     * The workspace this stable URL is initially bound to. Used only in Create
     * requests to associate the stable URL with a workspace. Not returned in
     * responses
     */
    declare public readonly initialWorkspaceId: pulumi.Output<string>;
    /**
     * (string) - Fully qualified resource name.
     * Format: accounts/{account_id}/stable-urls/{stable_url_id}
     */
    declare public /*out*/ readonly name: pulumi.Output<string>;
    /**
     * The parent resource. Format: accounts/{account_id}
     */
    declare public readonly parent: pulumi.Output<string>;
    /**
     * Client-provided identifier for the stable URL. Used to construct the
     * resource name as {parent}/stable-urls/{stable_url_id}
     */
    declare public readonly stableUrlId: pulumi.Output<string>;
    /**
     * (string) - The stable URL endpoint. Generated on creation and
     * immutable thereafter. For non-Private-Link workspaces this is
     * `https://<spog_host>/?c=<connection_id>`. For Private-Link workspaces
     * this is the per-connection hostname
     */
    declare public /*out*/ readonly url: pulumi.Output<string>;

    /**
     * Create a DisasterRecoveryStableUrl resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DisasterRecoveryStableUrlArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DisasterRecoveryStableUrlArgs | DisasterRecoveryStableUrlState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DisasterRecoveryStableUrlState | undefined;
            resourceInputs["failoverGroupName"] = state?.failoverGroupName;
            resourceInputs["initialWorkspaceId"] = state?.initialWorkspaceId;
            resourceInputs["name"] = state?.name;
            resourceInputs["parent"] = state?.parent;
            resourceInputs["stableUrlId"] = state?.stableUrlId;
            resourceInputs["url"] = state?.url;
        } else {
            const args = argsOrState as DisasterRecoveryStableUrlArgs | undefined;
            if (args?.initialWorkspaceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'initialWorkspaceId'");
            }
            if (args?.parent === undefined && !opts.urn) {
                throw new Error("Missing required property 'parent'");
            }
            if (args?.stableUrlId === undefined && !opts.urn) {
                throw new Error("Missing required property 'stableUrlId'");
            }
            resourceInputs["initialWorkspaceId"] = args?.initialWorkspaceId;
            resourceInputs["parent"] = args?.parent;
            resourceInputs["stableUrlId"] = args?.stableUrlId;
            resourceInputs["failoverGroupName"] = undefined /*out*/;
            resourceInputs["name"] = undefined /*out*/;
            resourceInputs["url"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DisasterRecoveryStableUrl.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DisasterRecoveryStableUrl resources.
 */
export interface DisasterRecoveryStableUrlState {
    /**
     * (string) - Fully qualified resource name of the FailoverGroup this stable URL is
     * currently linked to, in the format
     * `accounts/{account_id}/failover-groups/{failover_group_id}`. Empty when
     * the stable URL is not attached to any failover group
     */
    failoverGroupName?: pulumi.Input<string | undefined>;
    /**
     * The workspace this stable URL is initially bound to. Used only in Create
     * requests to associate the stable URL with a workspace. Not returned in
     * responses
     */
    initialWorkspaceId?: pulumi.Input<string | undefined>;
    /**
     * (string) - Fully qualified resource name.
     * Format: accounts/{account_id}/stable-urls/{stable_url_id}
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The parent resource. Format: accounts/{account_id}
     */
    parent?: pulumi.Input<string | undefined>;
    /**
     * Client-provided identifier for the stable URL. Used to construct the
     * resource name as {parent}/stable-urls/{stable_url_id}
     */
    stableUrlId?: pulumi.Input<string | undefined>;
    /**
     * (string) - The stable URL endpoint. Generated on creation and
     * immutable thereafter. For non-Private-Link workspaces this is
     * `https://<spog_host>/?c=<connection_id>`. For Private-Link workspaces
     * this is the per-connection hostname
     */
    url?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a DisasterRecoveryStableUrl resource.
 */
export interface DisasterRecoveryStableUrlArgs {
    /**
     * The workspace this stable URL is initially bound to. Used only in Create
     * requests to associate the stable URL with a workspace. Not returned in
     * responses
     */
    initialWorkspaceId: pulumi.Input<string>;
    /**
     * The parent resource. Format: accounts/{account_id}
     */
    parent: pulumi.Input<string>;
    /**
     * Client-provided identifier for the stable URL. Used to construct the
     * resource name as {parent}/stable-urls/{stable_url_id}
     */
    stableUrlId: pulumi.Input<string>;
}
