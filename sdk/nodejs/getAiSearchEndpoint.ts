// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * [![Public Preview](https://img.shields.io/badge/Release_Stage-Public_Preview-yellowgreen)](https://docs.databricks.com/aws/en/release-notes/release-types)
 *
 * [API Documentation](https://docs.databricks.com/api/workspace/aisearch)
 *
 * This data source can be used to get a single AI Search endpoint by name.
 *
 * ## Example Usage
 *
 * # Example: AI Search Endpoint Datasource (Singular)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as databricks from "@pulumi/databricks";
 *
 * const example = databricks.getAiSearchEndpoint({
 *     name: "example-ai-search-endpoint",
 * });
 * ```
 */
export function getAiSearchEndpoint(args: GetAiSearchEndpointArgs, opts?: pulumi.InvokeOptions): Promise<GetAiSearchEndpointResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("databricks:index/getAiSearchEndpoint:getAiSearchEndpoint", {
        "name": args.name,
        "providerConfig": args.providerConfig,
    }, opts);
}

/**
 * A collection of arguments for invoking getAiSearchEndpoint.
 */
export interface GetAiSearchEndpointArgs {
    /**
     * Name of the AI Search endpoint. Server-assigned full resource path
     * (`workspaces/{workspace}/endpoints/{endpoint}`) on output. On create, the
     * user-supplied short name is conveyed via `CreateEndpointRequest.endpoint_id`;
     * the server composes the full `name` and returns it on the response
     */
    name: string;
    /**
     * Configure the provider for management through account provider.
     */
    providerConfig?: inputs.GetAiSearchEndpointProviderConfig;
}

/**
 * A collection of values returned by getAiSearchEndpoint.
 */
export interface GetAiSearchEndpointResult {
    /**
     * (string) - The user-selected budget policy id for the endpoint
     */
    readonly budgetPolicyId: string;
    /**
     * (string) - Time the endpoint was created
     */
    readonly createTime: string;
    /**
     * (string) - Creator of the endpoint
     */
    readonly creator: string;
    /**
     * (list of CustomTag) - The custom tags assigned to the endpoint
     */
    readonly customTags: outputs.GetAiSearchEndpointCustomTag[];
    /**
     * (string) - The budget policy id applied to the endpoint
     */
    readonly effectiveBudgetPolicyId: string;
    /**
     * (EndpointStatus) - Current status of the endpoint
     */
    readonly endpointStatus: outputs.GetAiSearchEndpointEndpointStatus;
    /**
     * (string) - Type of endpoint. Required on create and immutable thereafter. Possible values are: `STANDARD`, `STORAGE_OPTIMIZED`
     */
    readonly endpointType: string;
    /**
     * (string) - Unique identifier of the endpoint
     */
    readonly id: string;
    /**
     * (integer) - Number of indexes on the endpoint
     */
    readonly indexCount: number;
    /**
     * (string) - User who last updated the endpoint
     */
    readonly lastUpdatedUser: string;
    /**
     * (string) - Name of the AI Search endpoint. Server-assigned full resource path
     * (`workspaces/{workspace}/endpoints/{endpoint}`) on output. On create, the
     * user-supplied short name is conveyed via `CreateEndpointRequest.endpoint_id`;
     * the server composes the full `name` and returns it on the response
     */
    readonly name: string;
    readonly providerConfig?: outputs.GetAiSearchEndpointProviderConfig;
    /**
     * (integer) - The client-supplied desired number of replicas for the endpoint, applied at
     * create/update time. Mutually exclusive with `targetQps`
     */
    readonly replicaCount: number;
    /**
     * (EndpointScalingInfo) - Scaling information for the endpoint
     */
    readonly scalingInfo: outputs.GetAiSearchEndpointScalingInfo;
    /**
     * (integer) - Target QPS for the endpoint. Mutually exclusive with `replicaCount`. Best-effort;
     * the system does not guarantee this QPS will be achieved
     */
    readonly targetQps: number;
    /**
     * (EndpointThroughputInfo) - Throughput information for the endpoint
     */
    readonly throughputInfo: outputs.GetAiSearchEndpointThroughputInfo;
    /**
     * (string) - Time the endpoint was last updated
     */
    readonly updateTime: string;
    /**
     * (string) - The usage policy id applied to the endpoint
     */
    readonly usagePolicyId: string;
}
/**
 * [![Public Preview](https://img.shields.io/badge/Release_Stage-Public_Preview-yellowgreen)](https://docs.databricks.com/aws/en/release-notes/release-types)
 *
 * [API Documentation](https://docs.databricks.com/api/workspace/aisearch)
 *
 * This data source can be used to get a single AI Search endpoint by name.
 *
 * ## Example Usage
 *
 * # Example: AI Search Endpoint Datasource (Singular)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as databricks from "@pulumi/databricks";
 *
 * const example = databricks.getAiSearchEndpoint({
 *     name: "example-ai-search-endpoint",
 * });
 * ```
 */
export function getAiSearchEndpointOutput(args: GetAiSearchEndpointOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetAiSearchEndpointResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("databricks:index/getAiSearchEndpoint:getAiSearchEndpoint", {
        "name": args.name,
        "providerConfig": args.providerConfig,
    }, opts);
}

/**
 * A collection of arguments for invoking getAiSearchEndpoint.
 */
export interface GetAiSearchEndpointOutputArgs {
    /**
     * Name of the AI Search endpoint. Server-assigned full resource path
     * (`workspaces/{workspace}/endpoints/{endpoint}`) on output. On create, the
     * user-supplied short name is conveyed via `CreateEndpointRequest.endpoint_id`;
     * the server composes the full `name` and returns it on the response
     */
    name: pulumi.Input<string>;
    /**
     * Configure the provider for management through account provider.
     */
    providerConfig?: pulumi.Input<inputs.GetAiSearchEndpointProviderConfigArgs | undefined>;
}
