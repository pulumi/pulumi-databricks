// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * [![Public Beta](https://img.shields.io/badge/Release_Stage-Public_Beta-orange)](https://docs.databricks.com/aws/en/release-notes/release-types)
 *
 * [API Documentation](https://docs.databricks.com/api/workspace/postgres)
 *
 * ## Example Usage
 *
 * ### Basic Synced Table with Snapshot Policy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as databricks from "@pulumi/databricks";
 *
 * const _this = new databricks.PostgresProject("this", {
 *     projectId: "my-project",
 *     spec: {
 *         pgVersion: 17,
 *         displayName: "My Project",
 *     },
 * });
 * const main = new databricks.PostgresBranch("main", {
 *     branchId: "main",
 *     parent: _this.name,
 *     spec: {
 *         noExpiry: true,
 *     },
 * });
 * const thisPostgresCatalog = new databricks.PostgresCatalog("this", {
 *     catalogId: "app_catalog",
 *     spec: {
 *         postgresDatabase: "app_db",
 *         createDatabaseIfMissing: true,
 *         branch: main.name,
 *     },
 * });
 * const thisPostgresSyncedTable = new databricks.PostgresSyncedTable("this", {
 *     syncedTableId: "app_catalog.default.users_synced",
 *     spec: {
 *         sourceTableFullName: "main.default.users",
 *         primaryKeyColumns: ["user_id"],
 *         schedulingPolicy: "SNAPSHOT",
 *         postgresDatabase: "app_db",
 *         branch: main.name,
 *         createDatabaseObjectsIfMissing: true,
 *         newPipelineSpec: {
 *             storageCatalog: "main",
 *             storageSchema: "default",
 *         },
 *     },
 * });
 * ```
 *
 * ### Synced Table with Triggered Policy
 *
 * Use `TRIGGERED` for on-demand updates. Requires Change Data Feed (CDF) enabled on the source table.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as databricks from "@pulumi/databricks";
 *
 * const triggered = new databricks.PostgresSyncedTable("triggered", {
 *     syncedTableId: "app_catalog.default.orders_synced",
 *     spec: {
 *         sourceTableFullName: "main.default.orders",
 *         primaryKeyColumns: ["order_id"],
 *         schedulingPolicy: "TRIGGERED",
 *         postgresDatabase: "app_db",
 *         branch: main.name,
 *         createDatabaseObjectsIfMissing: true,
 *         newPipelineSpec: {
 *             storageCatalog: "main",
 *             storageSchema: "default",
 *         },
 *     },
 * });
 * ```
 *
 * ### Synced Table with Existing Pipeline
 *
 * Bin-pack into an existing pipeline instead of creating a new one:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as databricks from "@pulumi/databricks";
 *
 * const _this = new databricks.PostgresSyncedTable("this", {
 *     syncedTableId: "app_catalog.default.products_synced",
 *     spec: {
 *         sourceTableFullName: "main.default.products",
 *         primaryKeyColumns: ["product_id"],
 *         schedulingPolicy: "SNAPSHOT",
 *         postgresDatabase: "app_db",
 *         branch: main.name,
 *         createDatabaseObjectsIfMissing: true,
 *         existingPipelineId: "abc123-def456",
 *     },
 * });
 * ```
 */
export class PostgresSyncedTable extends pulumi.CustomResource {
    /**
     * Get an existing PostgresSyncedTable resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: PostgresSyncedTableState, opts?: pulumi.CustomResourceOptions): PostgresSyncedTable {
        return new PostgresSyncedTable(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'databricks:index/postgresSyncedTable:PostgresSyncedTable';

    /**
     * Returns true if the given object is an instance of PostgresSyncedTable.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is PostgresSyncedTable {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === PostgresSyncedTable.__pulumiType;
    }

    /**
     * (string)
     */
    declare public /*out*/ readonly createTime: pulumi.Output<string>;
    /**
     * (string) - Output only. The Full resource name of the synced table in Postgres
     * where (catalog, schema, table) are the UC entity names.
     */
    declare public /*out*/ readonly name: pulumi.Output<string>;
    /**
     * Configure the provider for management through account provider.
     */
    declare public readonly providerConfig: pulumi.Output<outputs.PostgresSyncedTableProviderConfig>;
    /**
     * Configuration details of the synced table, such as the source table, scheduling policy, etc.
     * This attribute is specified at creation time and most fields are returned as is on subsequent queries
     */
    declare public readonly spec: pulumi.Output<outputs.PostgresSyncedTableSpec>;
    /**
     * (SyncedTableSyncedTableStatus) - Synced Table data synchronization status
     */
    declare public /*out*/ readonly status: pulumi.Output<outputs.PostgresSyncedTableStatus>;
    /**
     * The ID to use for the Synced Table. This becomes the final component of the SyncedTable's resource name.
     * ID is required and is the synced table name, containing (catalog, schema, table) tuple.
     * Elements of the tuple are the UC entity names.
     *
     * Example: "{catalog}.{schema}.{table}"
     *
     * syncedTableId represents both of the following:
     *
     * 1. An online VIEW virtual table in the Unity Catalog accessible via the Lakehouse Federation.
     * 2. Postgres table named "{table}" in schema "{schema}" in the connected Postgres database
     */
    declare public readonly syncedTableId: pulumi.Output<string>;
    /**
     * (string) - The Unity Catalog table ID for this synced table
     */
    declare public /*out*/ readonly uid: pulumi.Output<string>;

    /**
     * Create a PostgresSyncedTable resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: PostgresSyncedTableArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: PostgresSyncedTableArgs | PostgresSyncedTableState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as PostgresSyncedTableState | undefined;
            resourceInputs["createTime"] = state?.createTime;
            resourceInputs["name"] = state?.name;
            resourceInputs["providerConfig"] = state?.providerConfig;
            resourceInputs["spec"] = state?.spec;
            resourceInputs["status"] = state?.status;
            resourceInputs["syncedTableId"] = state?.syncedTableId;
            resourceInputs["uid"] = state?.uid;
        } else {
            const args = argsOrState as PostgresSyncedTableArgs | undefined;
            if (args?.syncedTableId === undefined && !opts.urn) {
                throw new Error("Missing required property 'syncedTableId'");
            }
            resourceInputs["providerConfig"] = args?.providerConfig;
            resourceInputs["spec"] = args?.spec;
            resourceInputs["syncedTableId"] = args?.syncedTableId;
            resourceInputs["createTime"] = undefined /*out*/;
            resourceInputs["name"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
            resourceInputs["uid"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(PostgresSyncedTable.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering PostgresSyncedTable resources.
 */
export interface PostgresSyncedTableState {
    /**
     * (string)
     */
    createTime?: pulumi.Input<string | undefined>;
    /**
     * (string) - Output only. The Full resource name of the synced table in Postgres
     * where (catalog, schema, table) are the UC entity names.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Configure the provider for management through account provider.
     */
    providerConfig?: pulumi.Input<inputs.PostgresSyncedTableProviderConfig | undefined>;
    /**
     * Configuration details of the synced table, such as the source table, scheduling policy, etc.
     * This attribute is specified at creation time and most fields are returned as is on subsequent queries
     */
    spec?: pulumi.Input<inputs.PostgresSyncedTableSpec | undefined>;
    /**
     * (SyncedTableSyncedTableStatus) - Synced Table data synchronization status
     */
    status?: pulumi.Input<inputs.PostgresSyncedTableStatus | undefined>;
    /**
     * The ID to use for the Synced Table. This becomes the final component of the SyncedTable's resource name.
     * ID is required and is the synced table name, containing (catalog, schema, table) tuple.
     * Elements of the tuple are the UC entity names.
     *
     * Example: "{catalog}.{schema}.{table}"
     *
     * syncedTableId represents both of the following:
     *
     * 1. An online VIEW virtual table in the Unity Catalog accessible via the Lakehouse Federation.
     * 2. Postgres table named "{table}" in schema "{schema}" in the connected Postgres database
     */
    syncedTableId?: pulumi.Input<string | undefined>;
    /**
     * (string) - The Unity Catalog table ID for this synced table
     */
    uid?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a PostgresSyncedTable resource.
 */
export interface PostgresSyncedTableArgs {
    /**
     * Configure the provider for management through account provider.
     */
    providerConfig?: pulumi.Input<inputs.PostgresSyncedTableProviderConfig | undefined>;
    /**
     * Configuration details of the synced table, such as the source table, scheduling policy, etc.
     * This attribute is specified at creation time and most fields are returned as is on subsequent queries
     */
    spec?: pulumi.Input<inputs.PostgresSyncedTableSpec | undefined>;
    /**
     * The ID to use for the Synced Table. This becomes the final component of the SyncedTable's resource name.
     * ID is required and is the synced table name, containing (catalog, schema, table) tuple.
     * Elements of the tuple are the UC entity names.
     *
     * Example: "{catalog}.{schema}.{table}"
     *
     * syncedTableId represents both of the following:
     *
     * 1. An online VIEW virtual table in the Unity Catalog accessible via the Lakehouse Federation.
     * 2. Postgres table named "{table}" in schema "{schema}" in the connected Postgres database
     */
    syncedTableId: pulumi.Input<string>;
}
