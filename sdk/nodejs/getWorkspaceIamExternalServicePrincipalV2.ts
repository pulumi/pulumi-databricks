// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * [![Public Beta](https://img.shields.io/badge/Release_Stage-Public_Beta-orange)](https://docs.databricks.com/aws/en/release-notes/release-types)
 *
 * [API Documentation](https://docs.databricks.com/api/workspace/iamv2)
 *
 * Retrieves an external service principal — a service principal that can be synced from your identity provider (IdP) — with the given external ID from the customer's IdP, scoped to a workspace. If the service principal does not exist in the account, it will be created. If the customer is not onboarded onto Automatic Identity Management (AIM), this returns an error.
 *
 * The `name` uses the format `external-service-principals/{external_service_principal_id}`, where `externalServicePrincipalId` is the service principal's object ID in the IdP (for example, a Microsoft Entra ID object ID).
 *
 * > **Note** Reading this data source has a side effect: it resolves the service principal against the IdP and provisions the service principal in the account if it does not already exist. Provisioning happens at the account level; it does not assign the service principal to the workspace.
 *
 * > **Note** This data source can be used with an account-level or workspace-level provider. With an account-level provider, a `workspaceId` is required — set it in the `providerConfig` block (or via the provider's `workspaceId` attribute). With a workspace-level provider, `workspaceId` is optional and defaults to the provider's workspace.
 *
 * ## Example Usage
 *
 * Referring to an external service principal within a workspace, using an account-level provider with the target workspace selected via `providerConfig`:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as databricks from "@pulumi/databricks";
 *
 * const example = databricks.getWorkspaceIamExternalServicePrincipalV2({
 *     name: "external-service-principals/11111111-2222-3333-4444-555555555555",
 *     providerConfig: {
 *         workspaceId: "1234567890123456",
 *     },
 * });
 * ```
 */
export function getWorkspaceIamExternalServicePrincipalV2(args: GetWorkspaceIamExternalServicePrincipalV2Args, opts?: pulumi.InvokeOptions): Promise<GetWorkspaceIamExternalServicePrincipalV2Result> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("databricks:index/getWorkspaceIamExternalServicePrincipalV2:getWorkspaceIamExternalServicePrincipalV2", {
        "name": args.name,
        "providerConfig": args.providerConfig,
    }, opts);
}

/**
 * A collection of arguments for invoking getWorkspaceIamExternalServicePrincipalV2.
 */
export interface GetWorkspaceIamExternalServicePrincipalV2Args {
    /**
     * The resource name of the external service principal. The format depends on
     * the API that returned it:
     * - Account-scoped: accounts/{account_id}/external-service-principals/{external_service_principal_id}
     * - Workspace-scoped: external-service-principals/{external_service_principal_id}
     */
    name: string;
    /**
     * Configure the provider for management through account provider.
     */
    providerConfig?: inputs.GetWorkspaceIamExternalServicePrincipalV2ProviderConfig;
}

/**
 * A collection of values returned by getWorkspaceIamExternalServicePrincipalV2.
 */
export interface GetWorkspaceIamExternalServicePrincipalV2Result {
    /**
     * (string) - The parent account ID, from Databricks
     */
    readonly accountId: string;
    /**
     * (string) - The activity status of the service principal in the Databricks account. Possible values are: `ACTIVE`, `INACTIVE`
     */
    readonly accountSpStatus: string;
    /**
     * (string) - Application ID of the service principal, from the customer's IdP
     */
    readonly applicationId: string;
    /**
     * (string) - Display name of the service principal, from the customer's IdP
     */
    readonly displayName: string;
    /**
     * (string) - The external ID of the service principal in the customer's IdP
     */
    readonly externalServicePrincipalId: string;
    /**
     * (string) - Internal servicePrincipalId of the service principal in Databricks
     */
    readonly internalId: string;
    /**
     * (string) - The resource name of the external service principal. The format depends on
     * the API that returned it:
     * - Account-scoped: accounts/{account_id}/external-service-principals/{external_service_principal_id}
     * - Workspace-scoped: external-service-principals/{external_service_principal_id}
     */
    readonly name: string;
    readonly providerConfig?: outputs.GetWorkspaceIamExternalServicePrincipalV2ProviderConfig;
}
/**
 * [![Public Beta](https://img.shields.io/badge/Release_Stage-Public_Beta-orange)](https://docs.databricks.com/aws/en/release-notes/release-types)
 *
 * [API Documentation](https://docs.databricks.com/api/workspace/iamv2)
 *
 * Retrieves an external service principal — a service principal that can be synced from your identity provider (IdP) — with the given external ID from the customer's IdP, scoped to a workspace. If the service principal does not exist in the account, it will be created. If the customer is not onboarded onto Automatic Identity Management (AIM), this returns an error.
 *
 * The `name` uses the format `external-service-principals/{external_service_principal_id}`, where `externalServicePrincipalId` is the service principal's object ID in the IdP (for example, a Microsoft Entra ID object ID).
 *
 * > **Note** Reading this data source has a side effect: it resolves the service principal against the IdP and provisions the service principal in the account if it does not already exist. Provisioning happens at the account level; it does not assign the service principal to the workspace.
 *
 * > **Note** This data source can be used with an account-level or workspace-level provider. With an account-level provider, a `workspaceId` is required — set it in the `providerConfig` block (or via the provider's `workspaceId` attribute). With a workspace-level provider, `workspaceId` is optional and defaults to the provider's workspace.
 *
 * ## Example Usage
 *
 * Referring to an external service principal within a workspace, using an account-level provider with the target workspace selected via `providerConfig`:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as databricks from "@pulumi/databricks";
 *
 * const example = databricks.getWorkspaceIamExternalServicePrincipalV2({
 *     name: "external-service-principals/11111111-2222-3333-4444-555555555555",
 *     providerConfig: {
 *         workspaceId: "1234567890123456",
 *     },
 * });
 * ```
 */
export function getWorkspaceIamExternalServicePrincipalV2Output(args: GetWorkspaceIamExternalServicePrincipalV2OutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetWorkspaceIamExternalServicePrincipalV2Result> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("databricks:index/getWorkspaceIamExternalServicePrincipalV2:getWorkspaceIamExternalServicePrincipalV2", {
        "name": args.name,
        "providerConfig": args.providerConfig,
    }, opts);
}

/**
 * A collection of arguments for invoking getWorkspaceIamExternalServicePrincipalV2.
 */
export interface GetWorkspaceIamExternalServicePrincipalV2OutputArgs {
    /**
     * The resource name of the external service principal. The format depends on
     * the API that returned it:
     * - Account-scoped: accounts/{account_id}/external-service-principals/{external_service_principal_id}
     * - Workspace-scoped: external-service-principals/{external_service_principal_id}
     */
    name: pulumi.Input<string>;
    /**
     * Configure the provider for management through account provider.
     */
    providerConfig?: pulumi.Input<inputs.GetWorkspaceIamExternalServicePrincipalV2ProviderConfigArgs | undefined>;
}
